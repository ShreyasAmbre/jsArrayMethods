/*
    REVERSE methods it help to used to reverse the array

    It modifyies the ORIGINAL ARRAY
*/

const numbers = [5, 4, 3, 2, 1];

let c = numbers.reverse()

console.log("REVERSE ARRAY", numbers) // as we can see in output original array is reversed
console.log("REVERSE ARRAY", c)



// WAP to reverse the words present in the string but string should not get reverse :- Capital of my country in India
 